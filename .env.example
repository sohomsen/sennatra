# Copy this file to .env.production and fill in your values before deploying.
# This file is safe to commit — it contains no secrets, only variable names.
#
# No application secrets are currently required by the sennatra codebase.
# Add NEXT_PUBLIC_* or server-side vars here as the app grows.

NODE_ENV=production
DOMAIN_NAME=yourdomain.com
